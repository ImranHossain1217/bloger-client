import React from "react";

const Loading = () => {
  return (
    <div className="flex justify-center items-center">
      <div className="border-2 p-10 border-green-600 border-dashed animate-spin rounded-full"></div>
    </div>
  );
};

export default Loading;
